// Intentionally empty.
//
// This previously hosted a native right-click / long-press recognizer used to
// switch containers. That approach conflicted with WKWebView's own gestures
// (text selection on iPadOS, and Plex's go-home gesture), so container
// switching now uses the floating menu button in ContainerHostView instead.
